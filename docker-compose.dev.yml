services:
  web:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ./sejongagro-web:/app
    ports:
      - "5173:5173"
    command: sh -c "npm ci && npm run dev -- --host 0.0.0.0"
